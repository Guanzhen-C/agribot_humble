/**:
  ros__parameters:
    pointCloudTopic: /lio_sam/points
    imuTopic: /imu/data
    odomTopic: /lio_sam/odometry/imu
    gpsTopic: /lio_sam/odometry/gps

    lidarFrame: lidar_link
    baselinkFrame: base_link
    odometryFrame: map
    mapFrame: map

    # The exported map georeference supplies absolute yaw after optimization.
    useImuHeadingInitialization: false
    # The tested RTK altitude failed to close by about 3 m after returning to
    # the start, so RTK constrains XY only and lidar/IMU/loops retain map Z.
    useGpsElevation: false
    gpsCovThreshold: 0.25
    # The official implementation otherwise skips GPS whenever the lidar-pose
    # covariance is already small. Zero keeps its built-in 5 m spacing but
    # guarantees that accepted fixed RTK measurements can constrain the graph.
    poseCovThreshold: 0.0

    savePCD: false
    savePCDDirectory: /agribot_maps/lio_sam/

    # The C16 adapter emits the Velodyne-compatible x/y/z/intensity/ring/time layout.
    sensor: velodyne
    N_SCAN: 16
    Horizon_SCAN: 1800
    downsampleRate: 1
    lidarMinRange: 0.30
    lidarMaxRange: 60.0

    # Conservative N300Pro values; replace these after a valid Allan run.
    imuAccNoise: 0.08
    imuGyrNoise: 0.001745329252
    imuAccBiasN: 0.00010
    imuGyrBiasN: 0.00010
    imuGravity: 9.80665
    imuRPYWeight: 0.01

    # N300Pro IMU origin expressed in the C16 lidar frame. LIO-SAM composes
    # lidarPose * lidar2Imu internally, so this is the inverse of the measured
    # IMU-to-lidar translation [0.3375, 0, 0.09] m.
    extrinsicTrans: [-0.3375, 0.0, -0.09]
    # Static level-floor calibration on 2026-08-04 used 1199 IMU samples and
    # 119 C16 scans. extrinsicRot maps IMU vectors into the lidar frame. The
    # ROS orientation is ENU<-IMU, so extrinsicRPY is the inverse rotation
    # used on the right side of that orientation quaternion.
    extrinsicRot: [0.999641422, 0.000025736, -0.026777346,
                   -0.000596335, 0.999772925, -0.021301254,
                   0.026770717, 0.021309584, 0.999414444]
    extrinsicRPY: [0.999641422, -0.000596335, 0.026770717,
                   0.000025736, 0.999772925, 0.021309584,
                   -0.026777346, -0.021301254, 0.999414444]

    edgeThreshold: 1.0
    surfThreshold: 0.1
    edgeFeatureMinValidNum: 10
    surfFeatureMinValidNum: 100

    odometrySurfLeafSize: 0.20
    mappingCornerLeafSize: 0.10
    mappingSurfLeafSize: 0.20

    z_tollerance: 2.0
    rotation_tollerance: 1.0

    numberOfCores: 8
    mappingProcessInterval: 0.15

    surroundingkeyframeAddingDistThreshold: 0.50
    surroundingkeyframeAddingAngleThreshold: 0.10
    surroundingKeyframeDensity: 1.0
    surroundingKeyframeSearchRadius: 50.0

    loopClosureEnableFlag: true
    loopClosureFrequency: 1.0
    surroundingKeyframeSize: 50
    historyKeyframeSearchRadius: 15.0
    historyKeyframeSearchTimeDiff: 30.0
    historyKeyframeSearchNum: 25
    historyKeyframeFitnessScore: 0.30

    globalMapVisualizationSearchRadius: 1000.0
    globalMapVisualizationPoseDensity: 5.0
    globalMapVisualizationLeafSize: 0.50
